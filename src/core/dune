(library
  (name            apero_core)
  (public_name     apero-core)  
  (wrapped        false)
  (libraries       lwt lwt.unix  logs logs.lwt logs.fmt logs.cli fmt fmt.cli fmt.tty cmdliner)
  (preprocess (pps ppx_deriving.show ppx_deriving.ord lwt_ppx)))

